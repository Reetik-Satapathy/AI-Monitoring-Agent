apiVersion: v1
kind: Service

metadata:
  name: {{ include "mysql.fullname" . }}

spec:
  selector:
    app: {{ include "mysql.name" . }}

  ports:
    - port: 3306
      targetPort: 3306

  type: ClusterIP